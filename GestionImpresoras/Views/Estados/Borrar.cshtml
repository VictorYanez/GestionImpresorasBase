@using GestionImpresoras.Models;
@model Estado

@{
    ViewData["Title"] = "Borrar Estado";
}

<h1>Borrar Estado </h1>
<h3>¿Estás seguro de que deseas borrar este Estado?</h3>
<h3>@Model.Nombre </h3>
<hr />
<form asp-controller="Estados" asp-action="Borrar">

    <!-- Definir el id que se envía desde la vista al controlador -->
    <input type="hidden" asp-for="Id" value="@Model.Id" class="Estados" />
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <div class="mb-3">
        <label asp-for="Nombre" class="form-label">Nombre</label>
        <input type="text" asp-for="Nombre" class="form-control" readonly />
        <!-- Utilizo la opción readonly para no permitir cambiar el contenido de los campos -->
        <span asp-validation-for="Nombre" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Descripcion" class="form-label">Descripcion</label>
        <input type="text" asp-for="Descripcion" class="form-control" readonly />
        <span asp-validation-for="Descripcion" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary "><i class="bi bi-eraser"></i> Enviar</button>
    <a class="btn btn-secondary" asp-action="Index"><i class="bi bi-backspace"></i> Cancelar</a>

</form>
